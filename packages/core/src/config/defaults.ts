import type { SlidesmithConfig } from './schema';

export const DEFAULT_CONFIG: SlidesmithConfig = {
  theme: 'dark-tech',
  ratio: '16:9',
  density: 'comfortable',
  output: 'output/deck.pptx',
  embedFonts: true,
};
